#include <iostream>
using namespace std;

void getPints(double pints[SIZE], double totalPints){
    int counter = 0;
    int totalPints = 0;
    for (counter < SIZE, counter++){
        totalPints = totalPints + pints[counter];
    }
    return totalPints;
}

int main(){
    const int SIZE = 7;
    int pints[SIZE];
    double totalPints;
    double averagePints = 0;
    double highPints = 0;
    double lowPints = 0;

    getTotal = getPints(pints[SIZE], totalPints);
    getTotal(double pints[SIZE], double totalPints);
    
}